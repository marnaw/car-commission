import React from 'react';
import { storiesOf } from '@storybook/react';
import RadioButton from './RadioButton';
storiesOf('atoms/Radio Button', module).add('Normal', () => (
  <RadioButton>Administrator</RadioButton>
));
